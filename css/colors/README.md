# Color Palettes

In this directory are various color palettes with SCSS variables, CSS variables, and CSS classes.

## CSS Variables

All CSS variables in all palettes share the prefix `--skc` for Site Kit color. This is followed by the palette name from the colors directory then the color name.

Example: `--skc--palette--color-name`.
